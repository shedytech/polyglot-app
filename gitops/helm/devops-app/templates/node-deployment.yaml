apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.node.name }}
spec:
  replicas: {{ .Values.node.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.node.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.node.name }}
    spec:
      containers:
      - name: {{ .Values.node.name }}
        image: {{ .Values.node.image.repository }}:{{ .Values.node.image.tag }}
        ports:
        - containerPort: {{ .Values.node.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.node.name }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.node.name }}
  ports:
    - port: 80
      targetPort: {{ .Values.node.port }}
      nodePort: {{ .Values.node.nodePort }}

